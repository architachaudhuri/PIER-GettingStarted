import { Admin } from "./admin";

export class Provider {
    firstName: string;
    lastName: string;
    salutation: string;
    providerId: string;
    location: string;
    admin: Admin;
}